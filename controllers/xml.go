package controllers

import "compressor/models"

// GenerateXML func will generate an xml
func GenerateXML(jams models.Jam) (*models.Project, error) {
	//var project models.Project{}
	return nil, nil
}
